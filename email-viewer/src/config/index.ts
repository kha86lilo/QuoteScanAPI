// Application configuration

export const config = {
  // Minimum date to show emails (emails before this date will be filtered out)
  MIN_EMAIL_DATE: '2025-12-02',

  // Pagination settings
  DEFAULT_PAGE_SIZE: 20,
  PAGE_SIZE_OPTIONS: [10, 20, 50, 100],
};

export default config;
